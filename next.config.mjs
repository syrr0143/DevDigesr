/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.daisyui.com', 'example.com', 'another-domain.com'], // List your domains here
    },
};

export default nextConfig;
