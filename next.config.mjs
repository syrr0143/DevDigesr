/** @type {import('next').NextConfig} */
const nextConfig = {
    images: {
        domains: ['img.daisyui.com', 'example.com', 'another-domain.com',"media.istockphoto.com"], // List your domains here
    },
};

export default nextConfig;
